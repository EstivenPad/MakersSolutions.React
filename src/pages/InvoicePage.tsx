
export const InvoicePage = () => {
    

    return (
        <div className="container">
            {/* <h2>Entities</h2>
            <table className="table table-striped table-bordered table-hover">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {entities.map(entity => (
                    <tr key={entity.id}>
                    <td>{entity.id}</td>
                    <td>{entity.name}</td>
                    <td>
                        <Link to={`/entity/${entity.id}`} className="btn btn-primary mr-2">Edit</Link>
                        <Button variant="danger" onClick={() => deleteEntity(entity.id)}>Delete</Button>
                    </td>
                    </tr>
                ))}
                </tbody>
            </table>
            <Link to="/entity/add" className="btn btn-success">Add Entity</Link> */}
        </div>
    )
}
